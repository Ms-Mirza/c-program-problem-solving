// union is same as structure but for union we can only use a member at a time.

#include<stdio.h>
int main()
{
    union book{
        char name[30];
        int page;
        float price;

    }
   
    
}